apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test
spec:
  rules:
  - host: base.lrnxp.com
    http:
      paths:
      - backend:
          serviceName: base
          servicePort: 80
